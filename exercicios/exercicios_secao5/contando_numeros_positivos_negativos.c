#include <stdio.h>

int main(){

int num, positivos, negativos, zeros;

positivos = 0;
negativos = 0;
zeros = 0;

printf}("Digite um valor: \n");
scanf("%d", &num);

if(num < 0)
	negativos--;
else if (num > 0)
	positivos++;
else
	zeros++;

printf}("Digite um valor: \n");
scanf("%d", &num);

if(num < 0)
        negativos--;
else if (num > 0)
        positivos++;
else
        zeros++;

printf}("Digite um valor: \n");
scanf("%d", &num);

if(num < 0)
        negativos--;
else if (num > 0)
        positivos++;
else
        zeros++;

printf}("Digite um valor: \n");
scanf("%d", &num);

if(num < 0)
        negativos--;
else if (num > 0)
        positivos++;
else
        zeros++;


printf}("Digite um valor: \n");
scanf("%d", &num);

if(num < 0)
        negativos--;
else if (num > 0)
        positivos++;
else
        zeros++;

printf("Positivos: %d\t\nNegativos: %d\t\nZeros: %d\t\n\n", positivos, negativos, zeros);


}
