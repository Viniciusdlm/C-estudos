#include <stdio.h>
int main(){
// A principal diferenca entre && e &  eh que um eh usado para fazer a diferenca entre dois testes  -> &&
// usamos para enderecar na regiao da memoria -> &
if(10<3 && 5<20){   //  aqui usamos a opcao de fazer os dois testes e comparar

}

int idade; //usamos para enderecar uma variavel na memoria
scanf("%d", &idade);


}
