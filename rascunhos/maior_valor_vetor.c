#include <stdio.h>

int main(void)
{
    int valores[] = {5, 8, 2, 11, 6};
    int maior = valores[0];

    /* TODO: percorrer o vetor e atualizar o maior valor encontrado. */
    printf("Maior valor inicial: %d\n", maior);

    return 0;
}
