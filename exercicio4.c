#include <stdio.h> //coloqo as bibliotecas que vou usar

int main()// inicio o codigo
{
    int A[] = {5,8,2,11,6};// aqui está o vetor
    int i; // aqui seria i referente ao indice para percorrer cada vetor
    int temp; //serve pra marcar o maior ate o momento
    
    temp = A[0]; //para dizer qual o maior numero encontrado

    for(i = 1; i < n; i++)// aqui seria para i = que é o indice , oq vai determinar o maior vai ser o temp
}
